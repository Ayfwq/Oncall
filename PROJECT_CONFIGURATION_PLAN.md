# 项目配置与接入规划

## 目标

一个 Project 不是一组随意填写的字段，而是一个可验证的监控目标：

`基本信息 → 监控范围 → 数据源连接 → 指标与告警 → 采集测试 → 启用`

保存只能产生“可解释的配置状态”，不能把缺少目标、无效地址或无效规则的半成品交给监控 worker。

## 配置模型

- **Project**：名称、用途说明、时区、采集周期、启停状态。
- **Target**：进程、日志、Docker、数据库、HTTP 服务；每个 target 独立启停。
- **Signal**：从已支持的指标目录选择采集指标，不允许前端随意拼写。
- **Rule**：指标、资源键、操作符、触发/恢复阈值、持续次数和级别。
- **Validation**：格式校验、跨字段校验、连接/采集 dry-run；连接密码只写入加密字段，不回显。

## 创建流程（下一阶段）

1. 选择项目名称、用途和采集周期。
2. 选择监控范围：主机、进程、日志、容器、数据库、HTTP 服务。
3. 只展示所选范围的配置卡片，并提供“测试此目标”。
4. 从指标目录生成推荐规则；用户可以调整阈值，但不能输入未知指标。
5. 展示配置摘要和风险提示（例如未配置规则、数据库不是只读账号）。
6. 执行一次 dry-run；所有目标通过或明确标记为“不可达但允许保存”后才允许启用。
7. 保存版本并进入监控；后续编辑生成新版本，保留变更记录和回滚入口。

## 当前版本与后续拆分

- 当前 V1：保留现有表单和 JSON 高级编辑；后端 DTO 已承担基础合法性校验，测试采集不落库。
- V1.1：把创建页改成向导，增加目标选择、规则模板和逐项测试。
- V1.2：引入 ProjectConfigVersion、差异对比、回滚和启用前门禁。
- V1.3：增加凭证轮换、只读权限检查和按环境（dev/staging/prod）复制配置。

## 完成标准

- 新建项目至少有明确名称和采集周期。
- 每个启用的 target 都能说明采集失败原因。
- 每条启用规则都引用已注册指标，触发/恢复参数有效。
- 保存、测试、启用三个动作状态独立，不能用“保存成功”冒充“监控已生效”。
- 飞书、Web 和 worker 使用同一份 Project runtime config，不能各自解释一套字段。
