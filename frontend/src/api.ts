export async function api<T>(path:string, init:RequestInit={}):Promise<T>{const r=await fetch('/api'+path,{...init,credentials:'include',headers:{'Content-Type':'application/json',...(init.headers||{})}});if(r.status===401){location.href='/login';throw new Error('unauthorized')}if(!r.ok)throw new Error(await r.text());return r.json()}
export async function streamChat(id:string,content:string,onEvent:(type:string,data:any)=>void){const r=await fetch(`/api/conversations/${id}/messages:stream`,{method:'POST',credentials:'include',headers:{'Content-Type':'application/json'},body:JSON.stringify({content})});if(!r.ok)throw new Error(await r.text());const reader=r.body!.getReader(),dec=new TextDecoder();let buf='';while(true){const {value,done}=await reader.read();if(done)break;buf+=dec.decode(value,{stream:true});let idx;while((idx=buf.indexOf('\n\n'))>=0){const block=buf.slice(0,idx);buf=buf.slice(idx+2);const lines=block.split('\n');const type=(lines.find(x=>x.startsWith('event:'))||'event: message').slice(6).trim();const raw=(lines.find(x=>x.startsWith('data:'))||'data: {}').slice(5).trim();onEvent(type,JSON.parse(raw))}}}
